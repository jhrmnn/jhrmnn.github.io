{
  "name": "jhrmnn-site-worker",
  "private": true,
  "description": "Cloudflare Worker: static site + single-actor ActivityPub server",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "typecheck": "tsc --noEmit"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20250101.0",
    "typescript": "^5.7.0",
    "wrangler": "^3.99.0"
  }
}
